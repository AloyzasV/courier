{% extends 'base.html.twig' %}

{% block title %}Hello HomePageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <a href="{{ path('flower') }}" class="btn btn-warning">Order flowers</a>
    <a href="{{ path('coffee') }}" class="btn btn-warning">Order coffee</a>
    <a href="{{ path('get_order_list', { 'type': 'json' }) }}" class="btn btn-primary">Get orders JSON</a>
    <a href="{{ path('get_order_list', { 'type': 'xml' }) }}" class="btn btn-primary">Get orders XML</a>
</div>
{% endblock %}
